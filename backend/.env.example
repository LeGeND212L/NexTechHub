# ============================================
# HOSTINGER PRODUCTION ENVIRONMENT VARIABLES
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=production

# Database - MongoDB Atlas Connection String
# Get this from: https://cloud.mongodb.com
# IMPORTANT: Make sure to:
# 1. Whitelist your Hostinger server IP in MongoDB Atlas Network Access
# 2. Or use 0.0.0.0/0 to allow all IPs (less secure but works)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority

# JWT Secret - Generate a strong random string (min 32 characters)
# You can generate one at: https://randomkeygen.com/
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_minimum_32_chars
JWT_EXPIRE=30d

# Email Configuration (for contact form & notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Frontend URL - Your Hostinger frontend domain
# Update this with your actual Hostinger domain
FRONTEND_URL=https://nextechubs.com

# Backend URL - Your Hostinger backend domain/subdomain
BACKEND_URL=https://next.nextechubs.com

# File Upload Settings
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# ============================================
# HOSTINGER DEPLOYMENT CHECKLIST:
# ============================================
# 1. Create .env file with these values (copy and rename this file)
# 2. Update MONGODB_URI with your actual MongoDB connection string
# 3. Update JWT_SECRET with a secure random string
# 4. Whitelist Hostinger IP in MongoDB Atlas (or use 0.0.0.0/0)
# 5. Run: npm install
# 6. Run: pm2 start ecosystem.config.js --env production
# 7. Run: pm2 save
# 8. Check logs: pm2 logs
# ============================================
